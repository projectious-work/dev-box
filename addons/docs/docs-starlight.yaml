name: docs-starlight
version: "1.0.0"
description: "Starlight – Astro-based docs site"
requires:
  - node

skills:
  - documentation

builder_weight: null

tools:
  - name: starlight
    default_enabled: true
    default_version: ""
    supported_versions: []

builder: null

runtime: |
  # Addon: docs-starlight
  RUN npm install -g create-starlight
