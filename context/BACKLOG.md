# Backlog

Central task registry. Each item has a unique ID for cross-referencing.
Source of truth — GitHub issues are for external visibility.
Archive of completed/merged items: [archive/BACKLOG.md](archive/BACKLOG.md)

## Next ID: BACK-074

## Format

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|

Status values: `todo`, `in-progress`, `done`, `blocked`, `archived`
Priority values: `must`, `should`, `could`, `wont`

---

## Active Items

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|
| BACK-002 | Security review | todo | must | **CLI:** input validation (container names, hostnames, package names, env names — injection vectors in generated Dockerfiles/compose); file ops (symlink following, path traversal in backup/restore/env); network calls (TLS, no credential leaks in update cmd); TOML config parsing (DoS via large/malformed input). **Container:** default root user (document implications, recommend non-root); mount permissions; Claude CLI install script (pins or hash verify); Dockerfile injection via user-controlled values (extra_packages, env vars, post_create_command); PulseAudio TCP without auth (document exposure). **Supply chain:** `cargo audit` on Cargo.lock; image provenance/cosign (sigstore/cosign for published images — was BACK-015); binary checksum verification in base Dockerfile (was BACK-014); skill hash verification + allowed-tools audit (was BACK-016); curl-pipe-bash install script (document verification). |
| BACK-004 | Skill eval framework | todo | should | Test/benchmark skills per Anthropic skill-creator pattern |
| BACK-006 | Docs review (existing-project, base-image pages) | todo | could | Deprioritized pending BACK-021 (Docusaurus migration — now done) and architecture changes (settled). Ready to action. |
| BACK-007 | Plugin / extension system | todo | should | **Scope:** (1) user-installable extensions that add commands, modify generation, or provide new features (oh-my-zsh model); (2) hook system — pre/post hooks for lifecycle commands (pre-build, post-start, etc.); (3) custom template overrides — user-provided Dockerfile.j2, docker-compose.yml.j2, layout .kdl templates; (4) community features — like devcontainer features but for aibox (shared via git repos or registry). **Open questions:** MVP plugin interface; shell scripts vs Rust dylibs vs WASM; toml config integration; distribution and versioning model. |
| BACK-008 | Zellij plugin integration | todo | should | **Investigate:** (1) existing Zellij plugin ecosystem — status bars, file managers, git integrations; (2) custom aibox plugin opportunity — status plugin showing container state, environment name, active theme, etc.; (3) plugin development UX — Zellij plugins are WASM, assess development friction; (4) integration points — how plugins are bundled in images and configured via layout KDL. Needs discussion with owner on scope before implementation. |
| BACK-011 | Remote development | todo | should | Environments on remote hosts, local thin client |
| BACK-017 | LaTeX addon package groups | todo | could | Add music (lilypond, musixtex), chemistry (chemfig, mhchem), linguistics tool groups to the latex add-on definition in addon_registry.rs |
| BACK-018 | AIUC-1 compliance alignment | todo | could | Awareness for aibox |
| BACK-019 | Skill marketplace integration | todo | could | ClawHub, Skills.sh |
| BACK-020 | `aibox doctor` skill consistency | todo | could | Check installed vs declared skills |
| BACK-024 | External skill installation | todo | could | Allow installing skills from sources outside curated list. Deprioritized. |
| BACK-025 | Skills gap analysis — internet research | todo | should | Research most-used agent skills/categories externally; research common SE/infra/docs/research tasks; compare with our 83 skills; identify gaps |
| BACK-026 | Existing skills quality review | todo | should | Audit all skills for missing examples, code snippets, and tools per SKILL.md format. Evaluate where tools would improve reliability (algorithmic vs probabilistic). Also audit skill descriptions for trigger optimization — Anthropic's research shows Claude tends to undertrigger; descriptions should be "a little pushy" (see context/ideas/competitive-landscape-2026-03.md R12) |
| BACK-027 | Three-level document structure audit | todo | should | Investigate whether all documents (project + derived project templates) follow the SKILL.md three-level rule (intro → overview → details) for context-efficient consumption by agents |
| BACK-029 | CLI output formats (table/JSON/YAML) | todo | could | Add structured output formats to list commands. Deprioritized, investigate later |
| BACK-030 | Bring-your-own-model AI provider support | todo | could | Allow custom/self-hosted model endpoints (e.g., vast.ai) in [ai] config. Deferred. |
| BACK-034 | New skill: software modularization | todo | should | Skill on keeping software in small, independent packages optimized for AI agent context limits. Covers module boundaries, package decomposition, API surface design for agent comprehension |
| BACK-035 | New skill: microservice creation & orchestration | todo | should | Skill on creating new services/microservices and orchestrating them. Service boundaries, inter-service communication, deployment patterns |
| BACK-042 | Internal project site for context documents | todo | could | Investigate and design a project-internal site that renders all markdown files in `./context/` as a browsable, nicely formatted site. Could be extended to a GitHub-independent wiki. **Intent:** collaboration for future multiple human workers, better readability of context files. Options: lightweight static site generator (e.g., Docusaurus second instance, mdBook, wiki.js), or extend existing docs-site with a context section. Should be simple to start — just rendered markdown with navigation |
| BACK-043 | Research additional AI provider integrations | todo | should | Research which additional AI coding agents/providers should be supported as addons. Minimum: **OpenAI Codex CLI** (open source, npm-based), **GitHub Copilot CLI**. Also evaluate: Cline, Continue.dev, Cursor CLI, Amazon Q Developer CLI, Cody. For each candidate: installation method, config directory, binary name, maturity level, license. Output: decision on which to add to addon_registry.rs |
| BACK-044 | Evaluate Mozilla cq integration | todo | should | Analyze https://github.com/mozilla-ai/cq and plan how to integrate it into aibox. Understand what cq does (code quality / context for AI agents?), how it relates to our skill and context system, whether it should be an addon, a skill, or a standalone tool. Research scope: architecture, integration points, value for aibox users, implementation plan |
| BACK-046 | Issue handling skill + agent architecture | todo | should | Investigate and design an issue handling system for aibox. **Skill scope:** When a user/customer reports a problem mid-work, the skill documents the issue properly and delegates to a separate issue-handling agent, interrupting ongoing work only if necessary. **Agent architecture:** Issue-handling agent runs independently, could handle customer communication, acts as sub-coordinator for per-customer issue agents (customer-specific treatment). **CLI integration:** Model-independent `/issue` command to trigger the workflow — report issue, assign priority, hand off to issue agent. **Research:** How to implement agent-to-agent handoff, per-customer agent instances, interrupt vs async handling, integration with existing backlog/tracking process package. Touches kaits orchestration boundary |
| BACK-047 | Investigate Docusaurus addon improvements | todo | could | The current docs-docusaurus addon installs `create-docusaurus` globally. Investigate: should it instead be a project-level `npx` pattern? Should the addon also scaffold a basic docs-site structure? How does it interact with the node addon (Docusaurus needs Node.js)? Should the addon auto-add the node addon as a dependency? |
| BACK-050 | Addon-skill mapping completeness + orphan skill audit | todo | should | 28 skill templates are currently orphaned (not in any process package or addon). New `skills:` field in addon YAML enables addon→skill auto-deployment. **Audit needed:** (1) Map remaining orphans to appropriate addons or packages (e.g., `sql-patterns` → future `postgres`/`database` addon?, `shell-scripting` → base?, `flutter-development` → future `flutter` addon?), (2) Decide: should some orphans become new packages (e.g., `data` package for `database-*`, `ml-pipeline`, `data-pipeline`)?, (3) Validate that addon-skill recommendations match user expectations (e.g., does every Python user want `fastapi-patterns`?). Related: BACK-025 (skills gap analysis), BACK-026 (skills quality review) |
| BACK-051 | Organizational learning via skill customization | todo | should | Design a mechanism for organizations/projects to encode their own conventions, preferences, and architectural decisions into skills. **Use case:** An org mandates "configuration over hardcoding" and prefers a specific config library/pattern — they should be able to say `aibox skill customize rust-conventions` and add org-specific rules that augment the curated skill content. **Design scope:** (1) Skill layering: curated base + project overlay (e.g., `.claude/skills/rust-conventions/CUSTOM.md` merged with template), (2) CLI UX: `aibox skill customize <name>` opens/creates the overlay, `aibox skill learn <name> "prefer clap over structopt"` appends a rule, (3) Propagation: how do team-wide conventions propagate vs project-local ones (git-committed overlays?), (4) Interaction with CLAUDE.md: should org rules live in skills, CLAUDE.md, or both? Consider skill as the structured channel and CLAUDE.md as the free-form one. **Motivation:** Skills currently encode generic best practices; real teams have specific, learned preferences (library choices, error handling patterns, naming conventions, CI/CD patterns) that should accumulate over time rather than be repeated each session. Related: BACK-007 (plugin/extension system), BACK-024 (external skill installation) |
| BACK-054 | Research VS Code themes for aibox | todo | should | Audit all themes we currently ship (gruvbox-dark, catppuccin-mocha, catppuccin-latte, dracula, tokyo-night, nord, projectious) and research the broader VS Code theme ecosystem. Questions: (1) Are the themes we ship actually available in the VS Code marketplace / open-vsix? (2) What are the most popular themes used by developers (download counts, community votes)? (3) Are there themes that pair well with our projectious brand? (4) Which themes have good multi-tool support (vim, zellij, starship, yazi, lazygit all need theme files)? (5) Should we add any new themes (e.g., One Dark Pro, Solarized, Monokai)? Output: decision on which themes to add/remove, and how to source VS Code theme files for devcontainer.json. |
| BACK-060 | Investigate aibox version upgrade flows | todo | must | Three flows to design and test: **(A)** user has version X project running → installs CLI version X+1 → wants to start with new configs/base container (what does `aibox start` do? detect mismatch? auto-migrate?). **(B)** same setup → user runs `aibox update` (should fetch latest image version, update aibox.toml, prompt to rebuild). **(C)** same setup → user edits aibox.toml manually → runs `aibox sync` → runs `aibox start` (should this validate version compatibility first?). **Key questions:** (1) Should the CLI detect that the running container was built from an older image version and warn? (2) Should `aibox start` refuse to start if the config version and image version diverge beyond a threshold? (3) What is the UX for each flow — silent, warn, or error? (4) Does `aibox doctor` already catch this, or do we need new checks? **Output:** decision document + implementation plan covering all three flows. |
| BACK-062 | Add starship prompts + asciinema previews for all themes and prompts in docs | todo | should | **(A) New prompts:** add 2-3 more Starship prompt presets. Specifically one that resembles Zellij's "arrow" style — powerline-style chevron segments showing directory, git branch, exit code etc. Also consider a "minimal" (single-line, no icons) variant. **(B) Asciinema recordings:** record a short cast for each prompt preset (current: default, minimal, nerd) showing it in action in the zellij layout. **(C) Docs:** add asciinema player embeds to the Themes and Prompts pages in Docusaurus — one cast per theme showing vim+zellij rendering, one cast per prompt showing the shell. **Implementation order:** design new prompts first → record casts → add to docs. See `scripts/record-asciinema.sh` and `docs-site/docs/customization/`. |
| BACK-064 | Add AI provider addons: OpenAI Codex CLI, GitHub Copilot CLI, Continue CLI | todo | must | Based on BACK-043 research (`context/research/ai-provider-integrations-2026-03.md`). Three new addons: **(1) `ai-openai`** — OpenAI Codex CLI (`npm install -g @openai/codex`, binary: `codex`, config: `~/.codex/`, auth: `OPENAI_API_KEY`). Open source (MIT), GA, large user base. **(2) `ai-copilot`** — GitHub Copilot CLI (`npm install -g @github/copilot`, binary: `copilot`, config: `~/.copilot/`, auth: GitHub Copilot subscription). GA since Feb 2026, Dev Container validated. **(3) `ai-continue`** — Continue CLI (`npm install -g @continuedev/cli`, binary: `cn`, config: `~/.continue/`, auth: `CONTINUE_API_KEY`). Provider-agnostic, Docker/headless-native, Apache 2.0. All three use npm install — same pattern as `ai-claude` and `ai-gemini`. **Also consider:** `ai-opencode` (lower priority, multi-provider TUI, `npm install -g opencode-ai`) — good for users who want a single tool across providers. **Implementation scope:** (1) create YAML files in `addons/ai/`; (2) add entries to `addon_registry.rs`; (3) add docs pages to `docs-site/docs/addons/`; (4) wire sidebar. |
| BACK-065 | Re-evaluate `ai-mistral` addon (SDK-only, not a CLI) | todo | must | The current `ai-mistral` addon installs the `mistralai` Python SDK — not an interactive coding CLI. This is categorically different from all other `ai-*` addons (Claude Code CLI, Gemini CLI, Aider, Codex CLI, Copilot CLI). **Options to decide between:** (1) Keep as-is with a clarifying comment and warning in docs that it is an SDK addon for programmatic use, not an interactive coding CLI; (2) Rename to `sdk-mistral` and introduce a new `addons/sdk/` category for SDK-only addons; (3) Remove and replace with a future Mistral interactive CLI if/when Le Chat CLI ships. **Research needed:** Has a Mistral interactive CLI emerged since March 2026? Is Le Chat CLI (noted as "in development" in BACK-043 research) now available? If yes, replace the SDK addon with the CLI. If no, decide on rename vs keep-as-is. |
| BACK-067 | New zellij layout: yazi-focused with large preview | done | should | "browse" layout: tab 1 yazi (60%) + AI pane (40%), tab 2 vim, tab 3 lazygit, tab 4 bash. Uses AIBOX_EDITOR_DIR=tab. |
| BACK-068 | Kubernetes deployment: scaffold Helm charts with pre-seeded values | todo | must | **Research needed:** (1) Which Kubernetes objects to scaffold — Deployment, Service, ConfigMap, Secret, PVC, Ingress, ServiceAccount, HPA? (2) What to make configurable via Helm values — image registry/tag, resource limits, replica count, env vars, volume mounts, ingress host, TLS, node selectors, tolerations? (3) How does this integrate with `aibox init` / `aibox sync` — new `--deploy k8s` flag? Separate `aibox deploy` command? (4) Should the Helm chart live in `.devcontainer/` or a dedicated `deploy/` directory? (5) Relationship to existing docker-compose — Kompose as starting point vs clean Helm scaffold? (6) How to handle secrets (sealed-secrets, external-secrets, SOPS)? **Output:** decision document + implementation plan. |
| BACK-069 | Skill definition versioning and status metadata | todo | should | Add `version` and `status` fields (alpha, beta, reviewed, stable) to SKILL.md frontmatter or a companion metadata file. **Scope:** (1) Define schema — where does version/status live (YAML frontmatter in SKILL.md? separate manifest?)? (2) CLI integration — `aibox skill list` shows version + status, `aibox skill info` displays full metadata. (3) Status lifecycle — who reviews, how does a skill move from alpha → stable? (4) Version semantics — semver for skills? Breaking change = new major? (5) Impact on skill deployment — should `aibox sync` warn when deploying alpha/beta skills? |
| BACK-070 | AI-provider-independent scheduled tasks | todo | should | **Research needed:** (1) How do different AI providers offer scheduled/recurring tasks — Claude Code triggers, Aider watch mode, Codex scheduled runs, GitHub Actions + AI, cron-based approaches? (2) Design a provider-agnostic scheduling abstraction — cron-like syntax in aibox.toml, or a dedicated skill that teaches agents to use system cron/systemd timers? (3) Use cases — scheduled code audits, recurring test runs, daily summaries, periodic dependency updates, overnight refactoring. (4) Should this be a CLI feature (`aibox schedule`) or a skill that instructs agents? (5) Integration with container lifecycle — do scheduled tasks survive container restart? **Output:** research report + design decision. |
| BACK-071 | Regular code audit process (simplification, security) | todo | should | Establish a recurring audit practice for the CLI codebase. **Scope:** (1) Simplification — identify dead code, over-abstractions, duplicated logic, unnecessary dependencies. (2) Security — review OWASP top 10 applicability, input validation coverage, supply chain (cargo audit, dependency review). (3) Performance — identify hot paths, unnecessary allocations, slow tests. (4) Process — how often (per release? monthly?), checklist template, tooling (clippy lints, cargo-deny, cargo-bloat, cargo-udeps). Consider making this a skill that can be triggered per session. Related: BACK-002 (security review). |
| BACK-072 | Docusaurus code walkthrough site for Rust newcomers | todo | should | **Planning + implementation:** A documentation site (or subsite under docs-site/) that explains the CLI codebase and architecture from the ground up for someone new to Rust but experienced in programming. **Scope:** (1) Architecture overview — module graph, data flow from `aibox init` → generated files, key abstractions. (2) Per-module walkthrough — config parsing, template rendering, addon system, seed/sync lifecycle, runtime detection. (3) Rust idioms used — Result/Option chains, trait objects, serde patterns, builder pattern, error handling with anyhow. (4) How to contribute — adding an addon, adding a command, adding a layout, writing tests. (5) Maintenance strategy — should the site auto-generate from doc comments (rustdoc?) or be hand-written? How to keep it in sync with code changes? |
| BACK-073 | Event log in ./context + event logging skill | todo | should | Add an event log (`context/EVENTS.md` or `context/events/`) that captures significant project events — deployments, incidents, decisions, milestones, handovers, configuration changes. **Scope:** (1) Event format — timestamp, category (deploy/incident/decision/milestone/config), summary, details. (2) Skill — teach agents when and how to log events (e.g., after a deployment, after resolving an incident, after a significant decision). (3) CLI integration — `aibox event add "deployed v0.13.1"` or purely skill-driven? (4) Relationship to DECISIONS.md — events are lighter-weight, chronological facts; decisions are heavier, with rationale and alternatives. (5) Process package — which packages include event logging (operations? managed? all?). |
| BACK-063 | File preview: images, SVG, vector, and PDF watch-mode in container | todo | must | **Context:** aibox containers run inside Zellij, which currently only passes through Sixel (Kitty/IIP are dropped — Zellij issues #4336/#3981). All preview solutions must either tolerate Sixel or use character-art fallback. **Three workstreams:** **(A) Yazi image/SVG/PDF preview** — install deps: `poppler-utils` (PDF via pdftoppm), `ffmpeg` (video thumbnails), `imagemagick` (HEIC/JPEG-XL), `chafa ≥1.16.0` (ASCII fallback); `resvg` binary for SVG (not in apt — install via upstream release or `cargo install resvg`). Set `image_delay=30` in yazi.toml to reduce Zellij Sixel artifacts. Verify with `yazi --debug`. Note: Sixel inside Zellij is CPU-intensive and artifact-prone — consider forcing chafa fallback via `TERM=xterm-kitty yazi` env wrapper. **(B) Standalone TUI image viewer** — add `chafa` + `timg` to base image. `chafa`: pure apt, no daemon, auto-detects Kitty/Sixel/IIP/halfblock, SVG via librsvg (verify `chafa --version` shows `SVG: yes`), AVIF/WebP, SSH-safe — best universal tool. `timg`: adds PDF-per-page rendering (`timg doc.pdf`) and video thumbnails. `viu` (Rust/Kitty-native) is optional — skip if chafa present. **(C) PDF watch-mode for LaTeX/Typst** — best headless approach: `entr`/`inotifywait` + `mutool draw` (from `mupdf-tools`) + `timg --clear`. Pattern: `ls output.pdf | entr -s 'mutool draw -o /tmp/p.png output.pdf 1 && timg --clear /tmp/p.png'`. Add `mupdf-tools` and `entr` to base image. `fancy-cat` (hot-reload, Kitty-native) is the ergonomic choice when host uses kitty/WezTerm but does NOT work inside Zellij. `tdf` (ratatui-based, character cells) works anywhere but low fidelity. GUI viewers (zathura, mupdf GUI) require X11 — not suitable for headless containers. **Implementation scope:** (1) add `chafa`, `timg`, `poppler-utils`, `mupdf-tools`, `entr`, `imagemagick`, `ffmpeg` to base Dockerfile; (2) configure yazi.toml image_delay + chafa fallback; (3) install resvg binary for SVG; (4) document the watch-preview pattern in docs. **⚡ Quick wins — image size (do first, next session):** Base image currently ships at >900 MB, ~465 MB above the original estimate. The preview dep group is the culprit — `ffmpeg` alone installs ~450 MB of codec libraries. Decisions: **(1) Remove `ffmpeg` from base image** — video thumbnails in yazi are not a priority; saves ~450 MB. Move to a new `preview-enhanced` addon for users who want video support. **(2) Remove `ghostscript`** — `mupdf-tools` (`mutool draw`) covers all PDF rendering needs in the base; ghostscript is redundant, saves ~70 MB. **(3) Review `poppler-utils`** — `mutool draw` can replace `pdftoppm` for most use cases; evaluate removing it (saves ~50 MB). **(4) Review `imagemagick`** — assess which formats actually need it vs. what chafa handles natively (AVIF/WebP/HEIC support in chafa depends on librsvg/libheif). **(5) Research Rust-native alternatives** — many heavy C/C++ tools are being re-implemented in Rust with smaller footprints. Research candidates: `oxipng`/`image-rs` instead of imagemagick for basic format conversion; `pdf-rs` or `lopdf` for PDF inspection; `svg2png` crates instead of resvg apt install; `inlyne` for document rendering. Prioritize tools with static musl binaries. **Target: get base image under 500 MB.** |
