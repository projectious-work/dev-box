# Backlog

Central task registry. Each item has a unique ID for cross-referencing.
Source of truth — GitHub issues are for external visibility.
Archive of completed/merged items: [archive/BACKLOG.md](archive/BACKLOG.md)

## Next ID: BACK-114

## Format

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|

Status values: `todo`, `in-progress`, `done`, `blocked`, `archived`
Priority values: `must`, `should`, `could`, `wont`

---

## Active Items

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|
| BACK-112 | v0.16.2 — `list_versions` GitHub Releases API fallback to `git ls-remote` | done | must | The unauthenticated GitHub API caps at 60 req/hour per IP. v0.16.1 picker had no fallback and silently fell through to `unset` when rate-limited, leaving users with the same v0.16.0 footgun. v0.16.2 falls through to `git ls-remote --tags --refs` on any failure (rate limit, network, JSON, empty). Same fallback shape as the install.sh fix. See DEC-029. |
| BACK-113 | v0.16.2 — sync perimeter catch-up with v0.16.1 auto-install | done | must | v0.16.1 wired `cmd_sync` to install processkit content (BACK-110) but `SYNC_PERIMETER` and `TRIPWIRE_SENTINELS` were not updated. Result: tripwire fired on AGENTS.md the first time a real `[processkit].version` was synced. v0.16.2 adds `aibox.lock`, `AGENTS.md`, `context/{skills,schemas,state-machines,processes,templates}/` to `SYNC_PERIMETER`; removes `AGENTS.md` from `TRIPWIRE_SENTINELS`. 6 new tests. See DEC-029. |
| BACK-110 | v0.16.1 — `aibox sync` auto-installs processkit content | done | must | **Bug:** v0.16.0 only installed processkit content from `cmd_init`. A user who ran `aibox init` with `[processkit].version = "unset"` (the default) and then edited `aibox.toml` to pin a real version would run `aibox sync` and get an empty `context/` with no error. **Fix:** new pure gating fn `container::sync_should_install_processkit(config_version, config_source, lock_pair)` returning true when config != unset AND (no lock OR lock disagrees with `(source, version)`). Wired into `cmd_sync` before the existing three-way diff. Five unit tests cover the gating decision. See DEC-028. |
| BACK-111 | v0.16.1 — `aibox init` interactive processkit version picker + CLI flags | done | should | New `content_source::list_versions(source) -> Result<Vec<String>>` (GitHub Releases API + `git ls-remote --tags` fallback, semver filter + sort). New `aibox init` flags: `--processkit-source`, `--processkit-version`, `--processkit-branch`. Resolver shows a `dialoguer::Select` with the latest at the top + an `unset — skip processkit install (configure later)` escape hatch at the bottom; non-interactive picks the latest; listing failure falls back to `unset` with a warning. Six unit tests cover the filter/sort/dedupe helpers. See DEC-028. |
| BACK-107 | v0.16.0 — rip bundled process layer | done | must | **Landed:** removed `cli/src/process_registry.rs`, `cli/src/skill_cmd.rs`, the entire `aibox skill` subcommand, the bundled `templates/` tree (~85 skills + context-doc scaffolds + processes + agents pointer), `context/AIBOX.md`, `ALL_SKILL_DEFS` and 141 `include_str!` calls in `cli/src/context.rs` (~2000 lines), `reconcile_skills`/`generate_aibox_md`/`check_agent_entry_points`, `effective_skill_includes`, `skills_for_addons`. Added `scaffolding/` install branch in `content_install.rs` so processkit's `scaffolding/AGENTS.md` lands at the project root. Inlined provider thin-pointer scaffolding (CLAUDE.md → AGENTS.md). Updated sync_perimeter to drop `context/AIBOX.md` and `.claude/skills/`. Bumped `[aibox].version` and `cli/Cargo.toml` to 0.16.0. See DEC-027. |
| BACK-108 | v0.16.0 — pin processkit v0.5.1 in this repo | done | must | Added `[processkit] version = "v0.5.1"` to the repo's own `aibox.toml`, switched `[context].packages` to `["product"]` (real processkit package). Replaced `templates/`-era preset names with the five processkit packages (`minimal`, `managed`, `software`, `research`, `product`). |
| BACK-109 | v0.16.0 — bump container deps queued from v0.15.0 | done | should | zellij `0.44.0 → 0.44.1`, fzf `0.70.0 → 0.71.0`, delta `0.19.1 → 0.19.2`, ouch `0.5.1 → 0.6.1`. Updated `images/base-debian/Dockerfile`, `.devcontainer/Dockerfile.e2e`, and `context/work-instructions/RELEASE-PROCESS.md`. |
| BACK-002 | Security review | todo | must | **CLI:** input validation (container names, hostnames, package names, env names — injection vectors in generated Dockerfiles/compose); file ops (symlink following, path traversal in backup/restore/env); network calls (TLS, no credential leaks in update cmd); TOML config parsing (DoS via large/malformed input). **Container:** default root user (document implications, recommend non-root); mount permissions; Claude CLI install script (pins or hash verify); Dockerfile injection via user-controlled values (extra_packages, env vars, post_create_command); PulseAudio TCP without auth (document exposure). **Supply chain:** `cargo audit` on Cargo.lock; image provenance/cosign (sigstore/cosign for published images — was BACK-015); binary checksum verification in base Dockerfile (was BACK-014); skill hash verification + allowed-tools audit (was BACK-016); curl-pipe-bash install script (document verification). |
| BACK-004 | Skill eval framework | todo | should | Test/benchmark skills per Anthropic skill-creator pattern |
| BACK-006 | Docs review (existing-project, base-image pages) | todo | could | Deprioritized pending BACK-021 (Docusaurus migration — now done) and architecture changes (settled). Ready to action. |
| BACK-007 | Plugin / extension system | todo | should | **Scope:** (1) user-installable extensions that add commands, modify generation, or provide new features (oh-my-zsh model); (2) hook system — pre/post hooks for lifecycle commands (pre-build, post-start, etc.); (3) custom template overrides — user-provided Dockerfile.j2, docker-compose.yml.j2, layout .kdl templates; (4) community features — like devcontainer features but for aibox (shared via git repos or registry). **Open questions:** MVP plugin interface; shell scripts vs Rust dylibs vs WASM; toml config integration; distribution and versioning model. |
| BACK-008 | Zellij plugin integration | todo | should | **Investigate:** (1) existing Zellij plugin ecosystem — status bars, file managers, git integrations; (2) custom aibox plugin opportunity — status plugin showing container state, environment name, active theme, etc.; (3) plugin development UX — Zellij plugins are WASM, assess development friction; (4) integration points — how plugins are bundled in images and configured via layout KDL. Needs discussion with owner on scope before implementation. |
| BACK-017 | LaTeX addon package groups | todo | could | Add music (lilypond, musixtex), chemistry (chemfig, mhchem), linguistics tool groups to the latex add-on definition in addon_registry.rs |
| BACK-018 | AIUC-1 compliance alignment | todo | could | Awareness for aibox |
| BACK-019 | Skill marketplace integration | todo | could | ClawHub, Skills.sh |
| BACK-020 | `aibox doctor` skill consistency | todo | could | Check installed vs declared skills |
| BACK-024 | External skill installation | todo | could | Allow installing skills from sources outside curated list. Deprioritized. |
| BACK-029 | CLI output formats (table/JSON/YAML) | todo | could | Add structured output formats to list commands. Deprioritized, investigate later |
| BACK-030 | Bring-your-own-model AI provider support | todo | could | Allow custom/self-hosted model endpoints (e.g., vast.ai) in [ai] config. Deferred. |
| BACK-034 | New skill: software modularization | todo | should | Skill on keeping software in small, independent packages optimized for AI agent context limits. Covers module boundaries, package decomposition, API surface design for agent comprehension |
| BACK-035 | New skill: microservice creation & orchestration | todo | should | Skill on creating new services/microservices and orchestrating them. Service boundaries, inter-service communication, deployment patterns |
| BACK-042 | Internal project site for context documents | todo | could | Investigate and design a project-internal site that renders all markdown files in `./context/` as a browsable, nicely formatted site. Could be extended to a GitHub-independent wiki. **Intent:** collaboration for future multiple human workers, better readability of context files. Options: lightweight static site generator (e.g., Docusaurus second instance, mdBook, wiki.js), or extend existing docs-site with a context section. Should be simple to start — just rendered markdown with navigation |
| BACK-062 | Add starship prompts + asciinema previews for all themes and prompts in docs | todo | should | **(A) New prompts:** add 2-3 more Starship prompt presets. Specifically one that resembles Zellij's "arrow" style — powerline-style chevron segments showing directory, git branch, exit code etc. Also consider a "minimal" (single-line, no icons) variant. **(B) Asciinema recordings:** record a short cast for each prompt preset (current: default, minimal, nerd) showing it in action in the zellij layout. **(C) Docs:** add asciinema player embeds to the Themes and Prompts pages in Docusaurus — one cast per theme showing vim+zellij rendering, one cast per prompt showing the shell. **Implementation order:** design new prompts first → record casts → add to docs. See `scripts/record-asciinema.sh` and `docs-site/docs/customization/`. |
| BACK-064 | Add AI provider addons: OpenAI Codex CLI, GitHub Copilot CLI, Continue CLI | todo | must | Based on BACK-043 research (`context/research/ai-provider-integrations-2026-03.md`). Three new addons: **(1) `ai-openai`** — OpenAI Codex CLI (`npm install -g @openai/codex`, binary: `codex`, config: `~/.codex/`, auth: `OPENAI_API_KEY`). Open source (MIT), GA, large user base. **(2) `ai-copilot`** — GitHub Copilot CLI (`npm install -g @github/copilot`, binary: `copilot`, config: `~/.copilot/`, auth: GitHub Copilot subscription). GA since Feb 2026, Dev Container validated. **(3) `ai-continue`** — Continue CLI (`npm install -g @continuedev/cli`, binary: `cn`, config: `~/.continue/`, auth: `CONTINUE_API_KEY`). Provider-agnostic, Docker/headless-native, Apache 2.0. All three use npm install — same pattern as `ai-claude` and `ai-gemini`. **Also consider:** `ai-opencode` (lower priority, multi-provider TUI, `npm install -g opencode-ai`) — good for users who want a single tool across providers. **Implementation scope:** (1) create YAML files in `addons/ai/`; (2) add entries to `addon_registry.rs`; (3) add docs pages to `docs-site/docs/addons/`; (4) wire sidebar. |
| BACK-065 | Re-evaluate `ai-mistral` addon (SDK-only, not a CLI) | todo | must | The current `ai-mistral` addon installs the `mistralai` Python SDK — not an interactive coding CLI. This is categorically different from all other `ai-*` addons (Claude Code CLI, Gemini CLI, Aider, Codex CLI, Copilot CLI). **Options to decide between:** (1) Keep as-is with a clarifying comment and warning in docs that it is an SDK addon for programmatic use, not an interactive coding CLI; (2) Rename to `sdk-mistral` and introduce a new `addons/sdk/` category for SDK-only addons; (3) Remove and replace with a future Mistral interactive CLI if/when Le Chat CLI ships. **Research needed:** Has a Mistral interactive CLI emerged since March 2026? Is Le Chat CLI (noted as "in development" in BACK-043 research) now available? If yes, replace the SDK addon with the CLI. If no, decide on rename vs keep-as-is. |
| BACK-076 | Support `"latest"` as explicit tool version to override pinning | todo | should | Allow users to set `version = "latest"` for any tool in `aibox.toml` (addon tools, AI provider tools) to explicitly opt out of version pinning. **Current behavior:** an empty version string (`""`) means "use addon default" — there is no way to say "always install the newest version, even if the addon defines a default pin." **Desired behavior:** `"latest"` is recognized as a sentinel value meaning "do not pass a version to the installer." Each addon runtime template would check `{% if tools.<name>.version and tools.<name>.version != "latest" %}` before emitting version-pinned install commands. **Scope:** (1) Reserve `"latest"` in config validation (`is_safe_version` in `config.rs`) — accept it but do not treat it as a literal version string. (2) Update `build_template_context` in `addon_loader.rs` to expose a `pinned` boolean or filter `"latest"` to empty so templates can branch. (3) Update all addon YAML templates that use version conditionals (currently: `ai-claude`, `python`, `node`, `rust`, `docs-docusaurus`). (4) Document in aibox.toml comments and docs-site. **Example:** `claude = { version = "latest" }` → installs without `-s <version>` flag; `claude = { version = "1.0.58" }` → installs with `-s 1.0.58`. |
| BACK-071 | Regular code audit process (simplification, security) | todo | should | Establish a recurring audit practice for the CLI codebase. **Scope:** (1) Simplification — identify dead code, over-abstractions, duplicated logic, unnecessary dependencies. (2) Security — review OWASP top 10 applicability, input validation coverage, supply chain (cargo audit, dependency review). (3) Performance — identify hot paths, unnecessary allocations, slow tests. (4) Process — how often (per release? monthly?), checklist template, tooling (clippy lints, cargo-deny, cargo-bloat, cargo-udeps). Consider making this a skill that can be triggered per session. Related: BACK-002 (security review). |
| BACK-072 | Docusaurus code walkthrough site for Rust newcomers | todo | should | **Planning + implementation:** A documentation site (or subsite under docs-site/) that explains the CLI codebase and architecture from the ground up for someone new to Rust but experienced in programming. **Scope:** (1) Architecture overview — module graph, data flow from `aibox init` → generated files, key abstractions. (2) Per-module walkthrough — config parsing, template rendering, addon system, seed/sync lifecycle, runtime detection. (3) Rust idioms used — Result/Option chains, trait objects, serde patterns, builder pattern, error handling with anyhow. (4) How to contribute — adding an addon, adding a command, adding a layout, writing tests. (5) Maintenance strategy — should the site auto-generate from doc comments (rustdoc?) or be hand-written? How to keep it in sync with code changes? |
| BACK-077 | Improve `aibox skill list` output + add `ls` alias | todo | should | **(1) Group and sort by category:** Skills should be grouped under their category heading (Process, Development, Language, Infrastructure, Architecture, Design, Data, AI/ML, API, Security, Observability, Database, Performance, Framework) and sorted alphabetically within each group. **(2) Short description:** Each skill should show a brief description (≤60 chars) so the listing is informative without line wrapping on standard 80-col terminals. Format: `  skill-name          — short description`. Source descriptions from SKILL.md frontmatter or hardcode in registry. **(3) `ls` alias:** Add `ls` as a short alias for `list` in the `aibox skill` subcommand (and consider doing the same for `aibox addon ls`). |
| BACK-078 | Review: preview companion design (PROJ-004 + BACK-063) | todo | must | Review `context/research/preview-companion-design-2026-03.md`. Two architectures to reconcile: **(A)** in-container tools (chafa, timg, mupdf-tools — already in base image, character-art quality) vs **(B)** companion container with web server + browser preview (pixel-perfect quality). Decide: keep both (hybrid), or companion-only? Discuss excalidraw file-bridge, SSH port forwarding UX, LaTeX edit→preview cycle. Decide on MVP scope. |
| BACK-079 | Review: remote development research (BACK-011) | todo | should | Review `context/research/remote-development-2026-03.md`. Discuss DevPod compatibility, `aibox start --remote` design, phasing. Decide on first deliverable. |
| BACK-080 | Review: skill customization design (BACK-051) | todo | should | Review `context/research/skill-customization-design-2026-03.md`. Discuss overlay approach, `aibox skill learn` command, org-wide sharing. Decide on Phase 1 scope. |
| BACK-081 | Review: Kubernetes/Helm deployment (BACK-068) | todo | should | Review `context/research/kubernetes-deployment-2026-03.md`. Discuss `aibox deploy k8s`, DevPod K8s provider, Helm chart structure, secret management. Decide on MVP. |
| BACK-082 | Review: event log design (BACK-073) | todo | should | Review `context/research/event-log-design-2026-03.md`. Discuss hybrid approach (skill + CLI), event categories, EVENTS.md format, process package placement. Decide on Phase 1. |
| BACK-083 | Review: skills gap analysis (BACK-025) | todo | should | Review `context/research/skills-gap-analysis-2026-03.md`. Compare aibox's 84 skills with external ecosystem, identify gaps, prioritize new skills to add. |
| BACK-084 | Review: Mozilla cq evaluation (BACK-044) | todo | should | Review `context/research/mozilla-cq-evaluation-2026-03.md`. Decide: integrate (addon/skill/tool) or skip. |
| BACK-085 | Review: VS Code themes (BACK-054) | todo | should | Review `context/research/vscode-themes-2026-03.md`. Decide which themes to add (3-5 new themes covering palette gaps). |
| BACK-086 | Review: version upgrade flows (BACK-060) | todo | must | Review `context/research/version-upgrade-flows-2026-03.md`. Discuss `aibox migrate` command, compatibility matrix, doctor checks. Decide on Phase 1. |
| BACK-087 | Review: scheduled tasks (BACK-070) | todo | should | Review `context/research/scheduled-tasks-2026-03.md`. Discuss provider-independent scheduling, container lifecycle, security. Decide on approach. |
| BACK-089 | Review: skills quality audit (BACK-026) | todo | should | Review `context/research/skills-quality-audit-2026-03.md`. Discuss quality patterns, top 10 skills needing improvement, trigger description fixes, authoring checklist. |
| BACK-090 | Review: document structure audit (BACK-027) | todo | should | Review `context/research/document-structure-audit-2026-03.md`. 44% non-compliant — discuss priority fixes (CLAUDE.md templates, DEVELOPMENT.md, PRD.md), approve authoring checklist. |
| BACK-091 | Review: issue handling design (BACK-046) | todo | should | Review `context/research/issue-handling-design-2026-03.md`. Discuss skill + CLI approach, agent delegation model, per-customer scope question. Decide on Phase 1. |
| BACK-092 | Review: addon-skill mapping audit (BACK-050) | todo | should | Review `context/research/addon-skill-mapping-audit-2026-03.md`. 31 orphaned skills (36%). Discuss: **(1)** Tier 1 — move 12 orphans to existing packages (low risk, do first). **(2)** Tier 3 "ai" package — adds a 15th category, consider deferring until demand signal. **(3)** Python/node addon skill validation — `fastapi-patterns` and `tailwind` forced on all users of those addons, should they be opt-in? **(4)** Missing `inter-agent-handover` template. |
| BACK-105 | Review: AI provider integrations (BACK-043) | todo | must | Review `context/research/ai-provider-integrations-2026-03.md`. Prioritize candidates: OpenAI Codex CLI and GitHub Copilot CLI (high), Continue CLI (medium), OpenCode (low). Discuss impact on ai-mistral SDK addon (BACK-065). Decide Phase 1 additions for BACK-064. |
| BACK-098 | Review: skill versioning design (BACK-069) | todo | should | Review `context/research/skill-versioning-design-2026-03.md`. Discuss frontmatter approach, 5-stage lifecycle, automated quality checks, `aibox skill check` command. |
| BACK-099 | Review: RAG layer design (BACK-088) | todo | should | Review `context/research/rag-context-layer-2026-03.md`. Discuss fastembed+sqlite-vec approach, `aibox search` CLI, MCP server for Claude Code, indexing strategy. |
| BACK-100 | Review: brand & design skills (BACK-093) | todo | should | Review `context/research/brand-design-skills-2026-03.md`. Discuss brand-package skill, category split (Brand & Design System vs UI & Visual), projectious-work/brand reference. |
| BACK-101 | Review: self-hosted AI models (BACK-094) | todo | should | Review `context/research/self-hosted-ai-models-2026-03.md`. Discuss `[ai.custom]` config, Ollama sidecar addon, Qwen 2.5 Coder findings, cost comparison. |
| BACK-102 | Review: owner profiling skill (BACK-095) | todo | should | Review `context/research/owner-profiling-skill-2026-03.md`. Discuss pure skill approach, OWNER.md extensions, privacy/consent model, draft SKILL.md. |
| BACK-103 | Review: Docusaurus addon improvements (BACK-047) | todo | could | Review `context/research/docusaurus-addon-improvements-2026-03.md`. Discuss removing global install, null runtime approach, docs-starlight same fix. |
| BACK-104 | Review: infrastructure provisioning (BACK-096) | todo | should | Review `context/research/infrastructure-provisioning-2026-03.md`. **Key boundary decision:** Does aibox own infrastructure provisioning or stay at the container layer? Research recommends staying at container layer + DevPod for IaC. Discuss: **(1)** Document DevPod compatibility as Phase 1 (zero code). **(2)** Ship reference Tofu modules for Hetzner/AWS as Phase 2? Or leave to users? **(3)** GPU config (`[container.gpu]`) — aibox concern or DevPod concern? **(4)** Hetzner as cheapest path (EUR 7.49/mo for dev server). |
| BACK-106 | Consume content sources via release-asset tarballs | done | must | **Direction confirmed 2026-04-07.** Replace today's "fetch git tarball + walk full repo with skip rules" with consumption of a `src/`-only release tarball attached to each processkit GitHub Release. **Why:** explicit shippable contract (processkit decides what's in the artifact), smaller/faster fetch, no skip rules needed in aibox's walker, bit-exact reproducibility via recorded asset SHA256. **Hybrid, not replacement:** keep git-tarball + git-clone fallback for users pinning to a branch / unreleased commit. **Scope:** (1) Extend `processkit_source::fetch` with a release-asset strategy: try `{source}/releases/download/{version}/processkit-{version}.tar.gz` first, fall back to existing git path on 404. (2) Compute and verify SHA256 against the sibling `.sha256` file when present. (3) Add `release_asset_sha256: Option<String>` field to `AiboxLock`, populated when the asset path was used. (4) Add `release_asset_url_template` to `[processkit]` in `aibox.toml` (default `"{source}/releases/download/{version}/processkit-{version}.tar.gz"`) so non-GitHub hosts can override. (5) Templates dir copy is unchanged — still verbatim mirror of whatever shape arrived. (6) Document the consumer/producer contract in `docs-site/`. **Cross-repo:** processkit must add a release script that builds the tarball and attaches it + checksum to each GH Release. Briefing handed to processkit agent on 2026-04-07. **Related:** DEC-025, the recently committed full-templates rework (commit `4c8bde3`). |
| BACK-097 | Review: process model retrospective — competitive landscape + collaboration experience | todo | must | Review `context/research/process-frameworks-research.md`, `context/research/competitive-landscape-2026-03.md`, and `context/research/competitive-tools-2026-03.md`. Revisit competitive landscape research, review process templates against real usage, evaluate SAFe/Shape Up, assess context system, reflect on collaboration patterns (parallel agents, backlog-driven work, session handovers). |
