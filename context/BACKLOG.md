# Backlog

Central task registry. Each item has a unique ID for cross-referencing.
Source of truth — GitHub issues are for external visibility.
Archive of completed/merged items: [archive/BACKLOG.md](archive/BACKLOG.md)

## Next ID: BACK-063

## Format

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|

Status values: `todo`, `in-progress`, `done`, `blocked`, `archived`
Priority values: `must`, `should`, `could`, `wont`

---

## Active Items

| ID | Title | Status | Priority | Notes |
|----|-------|--------|----------|-------|
| BACK-002 | Security review | todo | must | Sequenced after BACK-028 (done). **CLI:** input validation (container names, hostnames, package names, env names — injection vectors in generated Dockerfiles/compose); file ops (symlink following, path traversal in backup/restore/env); network calls (TLS, no credential leaks in update cmd); TOML config parsing (DoS via large/malformed input). **Container:** default root user (document implications, recommend non-root); mount permissions; Claude CLI install script (pins or hash verify); Dockerfile injection via user-controlled values (extra_packages, env vars, post_create_command); PulseAudio TCP without auth (document exposure). **Supply chain:** `cargo audit` on Cargo.lock; image provenance/cosign (see BACK-015); curl-pipe-bash install script (document verification). |
| BACK-004 | Skill eval framework | todo | should | Test/benchmark skills per Anthropic skill-creator pattern |
| BACK-006 | Docs review (existing-project, base-image pages) | todo | could | Deprioritized — do after Docusaurus migration (BACK-021) and architecture changes settle |
| BACK-007 | Plugin / extension system | todo | should | **Scope:** (1) user-installable extensions that add commands, modify generation, or provide new features (oh-my-zsh model); (2) hook system — pre/post hooks for lifecycle commands (pre-build, post-start, etc.); (3) custom template overrides — user-provided Dockerfile.j2, docker-compose.yml.j2, layout .kdl templates; (4) community features — like devcontainer features but for aibox (shared via git repos or registry). **Open questions:** MVP plugin interface; shell scripts vs Rust dylibs vs WASM; toml config integration; distribution and versioning model. |
| BACK-008 | Zellij plugin integration | todo | should | **Investigate:** (1) existing Zellij plugin ecosystem — status bars, file managers, git integrations; (2) custom aibox plugin opportunity — status plugin showing container state, environment name, active theme, etc.; (3) plugin development UX — Zellij plugins are WASM, assess development friction; (4) integration points — how plugins are bundled in images and configured via layout KDL. Needs discussion with owner on scope before implementation. |
| BACK-010 | Evaluate multi-service support approach | todo | could | Analysis only: should we support multi-service or leave to user? Factor in SSH/remote dev plans |
| BACK-011 | Remote development | todo | should | Environments on remote hosts, local thin client |
| BACK-012 | Session handover format | todo | should | Standard process template for AI session continuity; may already be done — needs review and gap analysis |
| BACK-014 | Binary checksum verification | todo | must | Verify downloaded binaries in base Dockerfile. Sequenced after architecture overhaul |
| BACK-015 | Image signing (cosign) | todo | must | sigstore/cosign for published images. Sequenced after architecture overhaul |
| BACK-016 | Skill security vetting | todo | must | Hash verification, allowed-tools audit. Sequenced after architecture overhaul |
| BACK-017 | LaTeX addon package groups | todo | could | Add music (lilypond, musixtex), chemistry (chemfig, mhchem), linguistics tool groups to the latex add-on definition in addon_registry.rs |
| BACK-018 | AIUC-1 compliance alignment | todo | could | Awareness for aibox |
| BACK-019 | Skill marketplace integration | todo | could | ClawHub, Skills.sh |
| BACK-020 | `aibox doctor` skill consistency | todo | could | Check installed vs declared skills |
| BACK-024 | External skill installation | todo | could | Allow installing skills from sources outside curated list. Deprioritized. |
| BACK-025 | Skills gap analysis — internet research | todo | should | Research most-used agent skills/categories externally; research common SE/infra/docs/research tasks; compare with our 83 skills; identify gaps |
| BACK-026 | Existing skills quality review | todo | should | Audit all skills for missing examples, code snippets, and tools per SKILL.md format. Evaluate where tools would improve reliability (algorithmic vs probabilistic). Also audit skill descriptions for trigger optimization — Anthropic's research shows Claude tends to undertrigger; descriptions should be "a little pushy" (see context/ideas/competitive-landscape-2026-03.md R12) |
| BACK-027 | Three-level document structure audit | todo | should | Investigate whether all documents (project + derived project templates) follow the SKILL.md three-level rule (intro → overview → details) for context-efficient consumption by agents |
| BACK-028 | Complete CLI/UX overhaul | done | must | 4 phases complete: (1) AI providers as addons in registry, (2) command simplification — `build`/`attach` removed, `sync` now seeds+generates+builds, (3) `aibox addon {list,add,remove,info}` with toml_edit, (4) `aibox skill {list,add,remove,info}` with toml_edit. kubectl-style resource commands (get/describe) deferred — current verb pattern sufficient |
| BACK-029 | CLI output formats (table/JSON/YAML) | todo | could | Add structured output formats to list commands. Deprioritized, investigate later |
| BACK-030 | Bring-your-own-model AI provider support | todo | could | Allow custom/self-hosted model endpoints (e.g., vast.ai) in [ai] config. Deferred. |
| BACK-031 | Revise process bundles from competitive research | done | should | Reviewed against SAFe/PMBOK/IPMA/CMMI frameworks. Current 13 packages + 4 presets are well-scoped for a dev environment tool. Event log pattern already captured (tracking package). Maturity model is kaits-scope. Process document structure (YAML frontmatter + section IDs) noted for future context file refinement. Process frameworks research promoted to work-instructions/PROCESS-ARCHITECTURE.md |
| BACK-032 | Clean up context/project-notes directory | done | should | Archived 5 completed session/plan files to archive/project-notes/. Kept architecture-skills-processes.md (core reference) and release-process.md (operational). Promoted process-frameworks-research.md to work-instructions/. Deleted ideas/.gitkeep |
| BACK-033 | Context ideas research review | done | should | Reviewed competitive-landscape-2026-03.md — key recommendations already captured: SKILL.md adoption (done), AIUC-1 (BACK-018), marketplace (BACK-019/024), skill trigger optimization added to BACK-026. No new backlog items needed |
| BACK-034 | New skill: software modularization | todo | should | Skill on keeping software in small, independent packages optimized for AI agent context limits. Covers module boundaries, package decomposition, API surface design for agent comprehension |
| BACK-035 | New skill: microservice creation & orchestration | todo | should | Skill on creating new services/microservices and orchestrating them. Service boundaries, inter-service communication, deployment patterns |
| BACK-039 | Develop visual identity | done | must | Complete: projectious.work brand system applied (Midnight #1d3352, Accent #E05232, Plus Jakarta Sans / Source Sans 3 / IBM Plex Mono). aibox logo (terminal brackets + sparkle stars) with full variant set (SVG, PNG 1x/2x/3x, favicon, apple-touch-icon). Docusaurus themed. "projectious" terminal theme for all tools. Social preview/OG image still todo. Retrospective: branding skill not needed — existing logo-design + frontend-design skills sufficient |
| BACK-040 | Analyse base image Dockerfile for multistage build optimization | done | should | All phases complete. Non-root user "aibox" + gosu entrypoint (session 2026-03-23b). Node multi-stage COPY in .devcontainer (session 2026-03-23b). Published image: 12 parallel BuildKit stages (10 tools + vim colors + fetch-base), per-tool cache invalidation, vim colorschemes in builder stage, apt groups documented with size estimates (~490 MB total). Apt kept as single RUN (splitting hurts more than helps). |
| BACK-042 | Internal project site for context documents | todo | could | Investigate and design a project-internal site that renders all markdown files in `./context/` as a browsable, nicely formatted site. Could be extended to a GitHub-independent wiki. **Intent:** collaboration for future multiple human workers, better readability of context files. Options: lightweight static site generator (e.g., Docusaurus second instance, mdBook, wiki.js), or extend existing docs-site with a context section. Should be simple to start — just rendered markdown with navigation |
| BACK-043 | Research additional AI provider integrations | todo | should | Research which additional AI coding agents/providers should be supported as addons. Minimum: **OpenAI Codex CLI** (open source, npm-based), **GitHub Copilot CLI**. Also evaluate: Cline, Continue.dev, Cursor CLI, Amazon Q Developer CLI, Cody. For each candidate: installation method, config directory, binary name, maturity level, license. Output: decision on which to add to addon_registry.rs |
| BACK-044 | Evaluate Mozilla cq integration | todo | should | Analyze https://github.com/mozilla-ai/cq and plan how to integrate it into aibox. Understand what cq does (code quality / context for AI agents?), how it relates to our skill and context system, whether it should be an addon, a skill, or a standalone tool. Research scope: architecture, integration points, value for aibox users, implementation plan |
| BACK-045 | E2E testing environment design | done | must | Design a secure end-to-end testing environment for CLI integration tests. **Approach:** docker-compose companion container accessible via SSH (preseeded keys) that simulates a user's host machine. Test scenarios: init→sync→edit→reset lifecycle, migration from earlier versions, file preservation (CLAUDE.md not deleted on reset), generated vs user-owned file boundaries. **Key constraint:** no docker socket exposure, no sibling containers from dev-container. **Open question:** how to securely simulate container creation from the companion (nested containers? podman rootless? mock runtime?). Needs architecture analysis |
| BACK-046 | Issue handling skill + agent architecture | todo | should | Investigate and design an issue handling system for aibox. **Skill scope:** When a user/customer reports a problem mid-work, the skill documents the issue properly and delegates to a separate issue-handling agent, interrupting ongoing work only if necessary. **Agent architecture:** Issue-handling agent runs independently, could handle customer communication, acts as sub-coordinator for per-customer issue agents (customer-specific treatment). **CLI integration:** Model-independent `/issue` command to trigger the workflow — report issue, assign priority, hand off to issue agent. **Research:** How to implement agent-to-agent handoff, per-customer agent instances, interrupt vs async handling, integration with existing backlog/tracking process package. Touches kaits orchestration boundary |
| BACK-047 | Investigate Docusaurus addon improvements | todo | could | The current docs-docusaurus addon installs `create-docusaurus` globally. Investigate: should it instead be a project-level `npx` pattern? Should the addon also scaffold a basic docs-site structure? How does it interact with the node addon (Docusaurus needs Node.js)? Should the addon auto-add the node addon as a dependency? |
| BACK-050 | Addon-skill mapping completeness + orphan skill audit | todo | should | 28 skill templates are currently orphaned (not in any process package or addon). New `skills:` field in addon YAML enables addon→skill auto-deployment. **Audit needed:** (1) Map remaining orphans to appropriate addons or packages (e.g., `sql-patterns` → future `postgres`/`database` addon?, `shell-scripting` → base?, `flutter-development` → future `flutter` addon?), (2) Decide: should some orphans become new packages (e.g., `data` package for `database-*`, `ml-pipeline`, `data-pipeline`)?, (3) Validate that addon-skill recommendations match user expectations (e.g., does every Python user want `fastapi-patterns`?). Related: BACK-025 (skills gap analysis), BACK-026 (skills quality review) |
| BACK-051 | Organizational learning via skill customization | todo | should | Design a mechanism for organizations/projects to encode their own conventions, preferences, and architectural decisions into skills. **Use case:** An org mandates "configuration over hardcoding" and prefers a specific config library/pattern — they should be able to say `aibox skill customize rust-conventions` and add org-specific rules that augment the curated skill content. **Design scope:** (1) Skill layering: curated base + project overlay (e.g., `.claude/skills/rust-conventions/CUSTOM.md` merged with template), (2) CLI UX: `aibox skill customize <name>` opens/creates the overlay, `aibox skill learn <name> "prefer clap over structopt"` appends a rule, (3) Propagation: how do team-wide conventions propagate vs project-local ones (git-committed overlays?), (4) Interaction with CLAUDE.md: should org rules live in skills, CLAUDE.md, or both? Consider skill as the structured channel and CLAUDE.md as the free-form one. **Motivation:** Skills currently encode generic best practices; real teams have specific, learned preferences (library choices, error handling patterns, naming conventions, CI/CD patterns) that should accumulate over time rather than be repeated each session. Related: BACK-007 (plugin/extension system), BACK-024 (external skill installation) |
| BACK-054 | Research VS Code themes for aibox | todo | should | Audit all themes we currently ship (gruvbox-dark, catppuccin-mocha, catppuccin-latte, dracula, tokyo-night, nord, projectious) and research the broader VS Code theme ecosystem. Questions: (1) Are the themes we ship actually available in the VS Code marketplace / open-vsix? (2) What are the most popular themes used by developers (download counts, community votes)? (3) Are there themes that pair well with our projectious brand? (4) Which themes have good multi-tool support (vim, zellij, starship, yazi, lazygit all need theme files)? (5) Should we add any new themes (e.g., One Dark Pro, Solarized, Monokai)? Output: decision on which themes to add/remove, and how to source VS Code theme files for devcontainer.json. |
| BACK-055 | Strengthen yazi `e`-key e2e assertion | done | must | Root causes found and fixed: (1) `DEFAULT_YAZI_KEYMAP` used `[manager]` but yazi 26.1.22 renamed it to `[mgr]` — keymap was silently ignored. (2) `zellij action` in background subshell targeted stale dead sessions without explicit `ZELLIJ_SESSION_NAME`. (3) Seeded keymap had `--confirm` blocking headless execution — removed. Fixed: seed.rs uses `[mgr]`, driver discovers active session via `zellij list-sessions --no-formatting`, uses `send-keys --pane-id 0` (terminal pane 0). Test passes at 7.7s. |
| BACK-056 | Add ANSI color-code assertions to visual theme tests | done | should | Visual theme tests in `visual.rs` only verify that the session starts (cast line count > N). They do not confirm that the correct color palette is applied. Fix: extract `\e[38;2;R;G;Bm` (true-color) escape sequences from the cast and assert that at least one color from each theme's palette appears. E.g. for gruvbox-dark assert `\e[38;2;235;219;178m` (fg1 #ebdbb2). This confirms vim/zellij render in the expected theme rather than falling back to defaults. Scope: one color-presence assertion per theme is sufficient to detect total theme failure; not a pixel-perfect check. |
| BACK-060 | Investigate aibox version upgrade flows | todo | must | Three flows to design and test: **(A)** user has version X project running → installs CLI version X+1 → wants to start with new configs/base container (what does `aibox start` do? detect mismatch? auto-migrate?). **(B)** same setup → user runs `aibox update` (should fetch latest image version, update aibox.toml, prompt to rebuild). **(C)** same setup → user edits aibox.toml manually → runs `aibox sync` → runs `aibox start` (should this validate version compatibility first?). **Key questions:** (1) Should the CLI detect that the running container was built from an older image version and warn? (2) Should `aibox start` refuse to start if the config version and image version diverge beyond a threshold? (3) What is the UX for each flow — silent, warn, or error? (4) Does `aibox doctor` already catch this, or do we need new checks? **Output:** decision document + implementation plan covering all three flows. |
| BACK-061 | Improve seeded config file comments — show all options inline | done | should | Rewrote `serialize_config_with_comments` in `container.rs` to follow PostgreSQL/Redis style: `===` section separators, inline trailing comments on active values, every option present (active or commented), copy-paste-ready commented-out options with valid values enumerated. Updated `Dockerfile.j2` with a header block explaining variables and customization. Updated `docker-compose.yml.j2` with section headers explaining volume mount strategy and customization entry points. All 236 unit tests pass; clippy clean. |
| BACK-062 | Add starship prompts + asciinema previews for all themes and prompts in docs | todo | should | **(A) New prompts:** add 2-3 more Starship prompt presets. Specifically one that resembles Zellij's "arrow" style — powerline-style chevron segments showing directory, git branch, exit code etc. Also consider a "minimal" (single-line, no icons) variant. **(B) Asciinema recordings:** record a short cast for each prompt preset (current: default, minimal, nerd) showing it in action in the zellij layout. **(C) Docs:** add asciinema player embeds to the Themes and Prompts pages in Docusaurus — one cast per theme showing vim+zellij rendering, one cast per prompt showing the shell. **Implementation order:** design new prompts first → record casts → add to docs. See `scripts/record-asciinema.sh` and `docs-site/docs/customization/`. |
| BACK-053 | Fix `aibox init` process selection — show presets only, not raw packages | done | must | `process_selection_items()` in `container.rs` now returns only presets; individual package names still accepted via `--process <name>` on the CLI. E2E tests added to `lifecycle.rs`: `init_with_managed_preset_creates_context_files` (verifies BACKLOG.md, DECISIONS.md, STANDUPS.md, session-template.md), `init_with_software_preset_creates_code_files` (verifies processes/ dir). |
| BACK-052 | Addon dependency tree — investigate and prototype | todo | should | Addons already have a `requires` field (added in fix/addon-dependency-ordering). **Investigate:** Is topological sort + explicit `requires` enough, or do we need a full SAT-solver-style dependency resolver? Concrete questions: (1) Can circular deps occur (A requires B, B requires A)? Detect and error cleanly. (2) Should `requires` be transitive (if python-addon requires build-tools, does the build-tools requirement propagate)? (3) Should we surface the full resolved install order in `aibox addon info` and `aibox init` output? (4) Are there cases where two addons are mutually exclusive (conflict detection)? If the tree is shallow (most addons have 0-1 deps), topological sort is sufficient and no additional complexity is needed — close with documentation. If deeper dependency chains emerge, prototype a recursive resolver. Start with analysis before coding. |
| BACK-059 | Upgrade e2e keybinding tests to use zellij 0.44 native CLI automation | done | must | Implemented in `visual_keybindings.rs` for the yazi-e test: `send-keys --pane-id <terminal_id>` for reliable pane targeting, `dump-screen --pane-id <id>` for output polling, session discovery via `zellij list-sessions --no-formatting | grep -v EXITED`. Key findings: `--pane-id` takes numeric terminal ID or `terminal_N` / `plugin_N` format (NOT pane name); session must be explicitly set to avoid targeting dead sessions. All 8 visual keybinding tests pass. |
| BACK-057 | Research systemd unit-file format for addon dependency design | todo | could | Study Linux systemd's `.service` unit-file format as a reference for how a mature system handles dependency declaration (`After=`, `Requires=`, `Wants=`, `Conflicts=`, `PartOf=` etc.). Extract lessons applicable to aibox addon YAML dependency fields. Related: BACK-052 (addon dependency tree investigation — reference this research when designing the final resolver). |
| BACK-058 | Fix `aibox update` 401 error in derived projects | done | must | `do_upgrade` now matches on `fetch_latest_image_version` error and warns + exits Ok instead of propagating with `?`. Error shows: "Could not fetch latest image version from registry: ... If the registry requires authentication, try: docker login ghcr.io". E2E tests added in `tests/e2e/update.rs`: `update_runs_without_crashing_in_derived_project` verifies exit 0 and no bare 401 in stderr; `update_check_exits_cleanly` covers `--check` mode. |
| BACK-049 | CLI `uninstall` command (self-removal) | done | must | Add `aibox uninstall` command that removes the CLI binary and any global config files (~/.aibox/ or similar). Danger zone UX like `reset`: confirmation prompt, --dry-run preview, --yes to skip prompt. Removes: CLI binary from PATH, cached addon definitions (if BACK-048 adds them), any global state. Does NOT remove project-level files (aibox.toml, .devcontainer/, context/) — that's what `reset` does. **Prerequisite for BACK-048:** if we add local config/cache files, we need a way to clean them up |
| BACK-048 | Externalize addon definitions from hardcoded Rust | done | must | Currently all 21 addon definitions (tools, versions, builder stages, runtime commands) are hardcoded in `addon_registry.rs` (~1300 lines of Rust). Every addon change requires a CLI rebuild. **Investigate:** Move addon definitions to declarative config files (YAML/TOML). **Distribution model:** (1) central definitions hosted in this GitHub repo (fetched/cached by CLI), (2) local fallback bundled with CLI binary for offline use. **Design scope:** addon definition format (metadata, tools, supported versions, Dockerfile snippets for builder + runtime stages), versioning and update mechanism, caching strategy, how the CLI resolves central vs local, community addon contributions. **Critical requirement:** rigorous version pinning for reproducibility — every tool in every addon must pin exact versions (not ranges, not `latest`), download URLs must include checksums (sha256), and the addon definition itself must be versioned so builds are reproducible across time. Current problems: Typst `0.14` vs `0.14.2` mismatch, Docusaurus installed deprecated v1 — both caused by imprecise version handling. Related: BACK-014 (binary checksum verification), BACK-024 (external skill installation), BACK-047 (Docusaurus addon) |
