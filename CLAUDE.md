# CLAUDE.md — aibox

> **Pointer file.** Canonical instructions live in [`AGENTS.md`](./AGENTS.md).
>
> Claude Code auto-loads `CLAUDE.md` on startup, so this thin file
> exists only to satisfy that convention. Edit `AGENTS.md`, not this
> file. Any changes here will not be picked up by other agent harnesses
> (Codex CLI, Continue, OpenCode, …).

Read **[`AGENTS.md`](./AGENTS.md)** for project instructions, build
commands, repository structure, and the aibox ⇄ processkit boundary.
